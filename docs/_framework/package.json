{
    "name": "fast-blazor-issue-template",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "start": "dotnet watch run",
        "build": "npm run build:app && npm run build:pages",
        "build:app": "dotnet publish -c Release",
        "build:pages": "rimraf docs && mkdirp docs && copyfiles -u 5 -E \"bin/Release/net5.0/publish/wwwroot/**/*\" docs",
        "build:pages:clean": "rimraf docs && mkdirp docs"
    },
    "devDependencies": {
        "copyfiles": "^2.4.1",
        "mkdirp": "^1.0.4",
        "rimraf": "^3.0.2"
    }
}
